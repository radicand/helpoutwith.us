mutation addOrgUserRole($organizationId: ID!, $email: String!, $role: String!) {
  addOrgUserRole(role: $role, organizationId: $organizationId, email: $email) {
    userId
    organizationId
    role
    organizationUserRoleId
  }
}
